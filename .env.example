# Postgres connection string от Neon или Supabase (с включённым pgvector)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Ключ Google AI Studio — https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...

# Опционально: второй ключ (другой проект Google AI Studio). Бесплатная квота
# считается per-project-per-model, так что отдельный проект даёт полностью
# независимый лимит запросов — src/lib/gemini.ts использует его вместо
# основного, если задан.
# GEMINI_API_KEY_2=AIza...

# Модель Gemini — при "лимит запросов исчерпан" в логах просто смени на
# другую (см. curl https://generativelanguage.googleapis.com/v1beta/models?key=$GEMINI_API_KEY)
GEMINI_MODEL=gemini-flash-lite-latest

# Опционально: ключ Groq (https://console.groq.com/keys) как альтернативный
# провайдер — src/lib/groq.ts, переключается в src/lib/summarizer.ts
# GROQ_API_KEY=gsk_...

# Сколько дней хранить новости перед удалением
RETENTION_DAYS=7

# Порог косинусной похожести для склейки новостей в один кластер (0-1, чем выше — тем строже)
DEDUPE_THRESHOLD=0.75
